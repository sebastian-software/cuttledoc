# pnpm configuration for native Node.js modules
#
# sherpa-onnx-node needs to be hoisted so it can find its binary dependencies.

# Hoist native modules that need to find their binaries
public-hoist-pattern[]=sherpa-onnx-*

# sherpa-onnx-node needs DYLD_LIBRARY_PATH set on macOS
# The cuttledoc CLI wrapper handles this automatically

